import React from 'react';
import {Typography} from '@mui/material';
import {Layout} from './layout';

export const InitialPage = () => (
  <Layout>
    <Typography variant='h4' >
      Greetings From PizzoSoft!
    </Typography>
  </Layout>
);